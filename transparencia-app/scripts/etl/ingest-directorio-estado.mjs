/**
 * Legacy placeholder intentionally disabled.
 *
 * The previous version contained hard-coded, synthetic ministerial data and
 * wrote it directly to the production D1 database. The public directory must
 * only be built from an official, versioned R2 release.
 */
throw new Error("LEGACY_SYNTHETIC_ETL_DISABLED: use an official R2 release");
