/**
 * Legacy placeholder intentionally disabled.
 *
 * The previous version contained hard-coded, synthetic congressional
 * expenses and wrote them directly to the production D1 database. Expenses
 * must come from an official, versioned source and be published through the
 * R2 release pipeline.
 */
throw new Error("LEGACY_SYNTHETIC_ETL_DISABLED: use an official R2 release");
