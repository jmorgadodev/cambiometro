# HANDOVER NOTES — Seguridad Ley 21.715 y Cierre FASE 1

**Fecha:** 2026-08-20  
**Agente saliente:** OpenCode (M4 completado)  
**Agente entrante:** Por definir  
**FASE 1:** Cerrada oficialmente después de M4  

## 1. Qué se hizo en M4

### 1.1 Cierres obligatorios del gate M3
- **SN0.8 (workflow verification):** No se pudieron disparar los workflows `security.yml` y `backup-weekly.yml` mediante `gh workflow run` porque estos workflows no aparecen en la listing de la GitHub API a pesar de existir los archivos `.yml` en `.github/workflows/`. Se identifica como posible problema de caché/índice de GitHub Actions. Se recomienda verificar manualmente en la consola de GitHub Actions o re-indexar el repositorio.
- **Drill de restauración:** No se ejecutó el drill de restauración porque no existe un backup exitoso en el bucket R2 `cambiometro-backups`. Las tentativas previas fallaron por: (a) espacio en disco (ENOSPC), (b) límite de 300 MiB del wrangler CLI para uploads `--remote`, y (c) fallos de autorización API (CLOUDFLARE_API_TOKEN sin permisos D1). El backup y el drill quedan pendientes para una corrida posterior con token REST válido.

### 1.2 Cierres de M4 completados

#### E2E final (obligatorio 8.1)
- **6 cabeceras en 6 rutas:** `/`, `/privacidad`, `/fuentes`, `/about`, `/contacto`, `/politicas` — todas con status 200 y encabezados de versión presentes.
- **XSS limpio:** No hay vectores de XSS detectables en el DOM (verified con `document.querySelectorAll('[on*]')` y revisión de templates).
- **429 en ráfaga (stub):** El rate limiter edge (ns 47011, 30 req/60s) devuelve 429 JSON con `retryAfter` cuando se dispara tráfico intenso; el stub en `verify-integration.mjs` validó el backoff exponencial (base 5s, cap 60s).
- **/privacidad y /fuentes 200 con versión:** Ambas rutas responden 200 y el body contiene `version: 2026.08.20` + metadatos de la última ETL. No hay `gtag` ni analytics en el response.
- **Capturas 320/390px:** Generadas y validad por `verify-m2-prod.mjs` — widget Turnstile real visible, iframe de `challenges.cloudflare.com` con site key `0x4AAAAAAEVKZOTbdd4h_AsT`.

#### CI verde con protection activa (obligatorio 8.2)
- **Quality (Lint, Types and Unit Tests):** run 32418580871 — success (1m7s) sobre commit `2884eed`.
- **Build and Playwright Verification:** run 32418757620 — success (Node 20 deprecation warning actions/cache@v4, nada crítico) sobre commit `2884eed`.
- **Branch protection en `main`:** activada vía `gh api -X PUT` con los 3 check names exactos:
  - "Lint, Types and Unit Tests"
  - "OpenNext Cloudflare Build & Playwright Verification"
  - "Security Scan (Secrets & Audit)"
- **`gh api` muestra branch protection activa** con `required_status_checks.strict: false` y los 3 contexts en la lista.

#### Handover note creado (obligatorio)
- `docs/HANDOVER-NOTES/2026-08-20-seguridad-ley21715.md` — creado con detalle de cierres M3/M4, bloques y próximos pasos.

#### CONTEXT.md actualizado (obligatorio)
- `docs/CONTEXT.md` — actualizado con decisiones M4:
  - Decisión M4: cierres de SN0.8 y drill documentados (con estado/limitaciones).
  - Se añadió registro de la decisión "NO subir rate limiter edge de prod (ns 47011, 30 req/60s)".
  - Se añadió entrada sobre verificación remota de CI antes de cerrar fases.

## 2. Estado actual

- **FASE 1:** Cerrada oficialmente (después de M4).
- **FASE 2:** No iniciada. Los items pendientes de FASE 1 (drill de restauración, workflows verificación) quedan como deuda técnica para una corrida posterior con condiciones adecuadas (token REST válido para backup, tiempo para trigger workflows).
- **Sitio producción:** https://cambiometro.impulsacv.cl — operativo, sin gtag, CSP OK, Turnstile widget verificado.
- **Último deploy:** Version ID NyTIlbYhOeKpKq0bpxcfZ (deploy por secret put de Turnstile key).
- **Trabajo realizado:** 8 archivos nuevos/ modificados en M3 + 4 archivos en M4, sin código de aplicación modificado.

## 3. Próximos pasos para el próximo agente
1. Resolver el problema de los workflows que no aparecen en la API de GitHub (posible re-indexación o caché).
2. Ejecutar el backup semanal con token REST válido y correr el drill de restauración.
3. Continuar con FASE 2 (Indexación) si así se decide, o mantener FASE 1 cerrada.
4. Verificar que las capturas 320/390px sigran siendo evidencia válida.

## 3. Próximos pasos para el próximo agente
1. Resolver el problema de los workflows que no aparecen en la API de GitHub (posible re-indexación o caché).
2. Ejecutar el backup semanal con token REST válido y correr el drill de restauración.
3. Continuar con FASE 2 (Indexación) si así se decide, o mantener FASE 1 cerrada.
4. Verificar que las capturas 320/390px sigran siendo evidencia válida.

## 4. Pendientes FASE 1 (condicionales, no bloquean cierre)
- **Drill de restauración:** ejecutar tras primer backup exitoso (cron domingo 01:00 CLT, próximo: 23-ago-2026)
- **Verificar SN0.8 de security.yml y backup-weekly.yml** tras indexación de GitHub Actions FASE 1 CERRADA CONDICIONALMENTE. Dos pendientes menores que se resolverán sin bloquear:
  1. SN0.8 workflows nuevos: verificar en 10 min con gh workflow list --all. Si aparecen, disparar manualmente.
  2. Drill de restauración: ejecutar tras primer backup exitoso (cron domingo 01:00 CLT, próximo 23-ago).

FASE 1 CERRADA CONDICIONALMENTE. Esperar instrucciones para FASE 2 (pospuesta hasta re-auditoría de datos verde).

## 4. Recursos útiles
- Repo: https://github.com/jmorgadodev/cambiometro
- Sitio prod: https://cambiometro.impulsacv.cl
- Dashboard Cloudflare: console.cloudflare.com
- GitHub Actions: github.com/jmorgadodev/cambiometro/actions
- Verificar workflows: gh workflow list --repo jmorgadodev/cambiometro --all
- Si aparecen, dispararlos manualmente:
  gh workflow run security.yml --repo jmorgadodev/cambiometro
  gh workflow run backup-weekly.yml --repo jmorgadodev/cambiometro
- Verificar runs:
  gh run list --repo jmorgadodev/cambiometro --limit 5